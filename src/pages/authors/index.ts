// AuthorsPage is loaded lazily by its route (code splitting) — not re-exported
// here, so the barrel import in App.tsx only pulls in the route registration.
export { authorsRoute } from './model/route'
